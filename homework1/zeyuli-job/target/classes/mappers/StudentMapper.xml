<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mapper.StudentMapper">
    
    <!-- 结果映射 -->
    <resultMap id="studentResultMap" type="com.example.pojo.Student">
        <id property="id" column="id"/>
        <result property="age" column="age"/>
        <result property="name" column="name"/>
        <result property="classId" column="class_id"/>
        <!-- 关联班级信息 -->
        <association property="classInfo" javaType="com.example.pojo.ClassInfo">
            <id property="id" column="class_id"/>
            <result property="className" column="class_name"/>
        </association>
    </resultMap>
    
    <!-- 根据ID查询学生 -->
    <select id="findById" resultMap="studentResultMap">
        SELECT 
            s.id, s.age, s.name, s.class_id,
            c.class_name
        FROM s_student s
        LEFT JOIN s_class c ON s.class_id = c.id
        WHERE s.id = #{id}
    </select>
    
    <!-- 查询所有学生 -->
    <select id="findAll" resultMap="studentResultMap">
        SELECT 
            s.id, s.age, s.name, s.class_id,
            c.class_name
        FROM s_student s
        LEFT JOIN s_class c ON s.class_id = c.id
    </select>
    
    <!-- 更新学生信息 -->
    <update id="update">
        UPDATE s_student
        <set>
            <if test="age != null">
                age = #{age},
            </if>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="classId != null">
                class_id = #{classId}
            </if>
        </set>
        WHERE id = #{id}
    </update>
    
    <!-- 插入学生 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO s_student (age, name, class_id)
        VALUES (#{age}, #{name}, #{classId})
    </insert>
    
    <!-- 删除学生 -->
    <delete id="deleteById">
        DELETE FROM s_student WHERE id = #{id}
    </delete>
    
</mapper>
